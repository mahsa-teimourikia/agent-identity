# Teaching configuration fragment — adapt to your deployment.
agent {
  data_dir = "/opt/spire/data/agent"
  log_level = "INFO"
  server_address = "spire-server"
  server_port = "8081"
  trust_bundle_path = "/opt/spire/conf/agent/bootstrap.crt"
  trust_domain = "example.org"
}
