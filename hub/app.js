const cards=document.querySelector('#lessons'),ws=document.querySelector('#workspace');
function render(level='all'){cards.innerHTML=LESSONS.filter(x=>level==='all'||x.level===level).map(x=>`<article class="card"><button data-i="${LESSONS.indexOf(x)}"><span class="badge">${x.level} · step ${x.step}</span><h3>${x.title}</h3><p>${x.summary}</p></button></article>`).join('');document.querySelectorAll('.card button').forEach(b=>b.onclick=()=>open(+b.dataset.i))}
function open(i){const x=LESSONS[i];ws.hidden=false;document.querySelector('#level').textContent=x.level.toUpperCase()+` · STEP ${x.step}`;document.querySelector('#title').textContent=x.title;document.querySelector('#summary').textContent=x.summary;document.querySelector('#learn').innerHTML=`<h3>Concept summary</h3><p>${x.detail}</p><p><a href="${x.material}" target="_blank" rel="noreferrer">Read the complete theory on GitHub →</a></p><p>After reading, use the Lab tab to implement the control and the Checkpoint tab to verify your understanding.</p>`;document.querySelector('#lab').innerHTML=`<h3>Practical lab</h3><p>${x.detail}</p><p>Run locally from the repository root:</p><pre>${x.run}</pre><p><a href="${x.lab}" target="_blank" rel="noreferrer">Open the Python source →</a>${x.notebook?` · <a href="${x.notebook}" target="_blank" rel="noreferrer">Open the notebook →</a>`:''}</p><p class="muted">The examples use synthetic data and the Python standard library; no API keys are required.</p>`;document.querySelector('#checkpoint').innerHTML=`<h3>Checkpoint</h3><p>Answer the question, then reveal the explanation and revisit the linked lesson if needed.</p><div class="question"><strong>${x.q[0]}</strong>${x.q[1].map((a,j)=>`<label><input type="radio" name="q" value="${j}"> ${a}</label>`).join('')}<button id="check">Check answer</button><p id="feedback"></p></div>`;document.querySelector('#check').onclick=()=>{const v=document.querySelector('input[name=q]:checked');const f=document.querySelector('#feedback');if(v&&+v.value===x.q[2]){f.textContent=\`Correct — \${x.q[3]}\`}else{f.innerHTML=\`Review — <strong>Correct answer:</strong> \${x.q[1][x.q[2]]}<br>\${x.q[3]}\`};f.className='result'};ws.scrollIntoView({behavior:'smooth'})}
document.querySelectorAll('nav button').forEach(b=>b.onclick=()=>{document.querySelectorAll('nav button').forEach(x=>x.classList.remove('active'));b.classList.add('active');render(b.dataset.level)});document.querySelectorAll('.tabs button').forEach(b=>b.onclick=()=>{document.querySelectorAll('.tabs button').forEach(x=>x.classList.remove('active'));b.classList.add('active');['learn','lab','checkpoint'].forEach(id=>document.querySelector('#'+id).hidden=id!==b.dataset.tab)});render();
